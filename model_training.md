The model is a simple neural network with an embedding layer and one linear layer. The inputs are sequences of three letters (front, right, top), encoded as tensors. The model is trained for 200 epochs using the Adam optimizer and CrossEntropyLoss. Each epoch computes the loss, updates the parameters, and logs results (accuracy and training time). This ensures that the experiment is reproducible and the performance metrics are consistently evaluated
